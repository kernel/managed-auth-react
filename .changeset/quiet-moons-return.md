---
"@onkernel/managed-auth-react": patch
---

Keep an already-ready login form visible when the consent step is dismissed, instead of replacing it with a loading state that no later event will clear.
